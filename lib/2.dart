import 'package:flutter/material.dart';

class Tabbar extends StatelessWidget{
  Tabbar(PageStorageKey<String> pageStorageKey  );

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      backgroundColor: Colors.yellow,

    );

  }
}